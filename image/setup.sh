#!/usr/bin/env bash
# Per-launch, per-sandbox setup. Re-runnable by hand as `sandbox-setup`.
# No-op by default; add project-specific setup here. A configured k3s cluster is
# already starting by the time this runs (see entrypoint.sh).
set -euo pipefail
exit 0
