#!/usr/bin/env bash
set -euo pipefail

REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)"
cd "$REPO"

{
  find image -type f -print0 | sort -z | xargs -0 shasum
  shasum config.toml
} | shasum | awk '{print $1}'
